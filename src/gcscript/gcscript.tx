// Structure

Root:
    imports+=Import
    classes+=ClassDef
;

Comment: /\/\/.*$/;

Import:
    'import' path+=ID+'.' name=ID
;

// Enums
Scope: 'public' | 'private';

// Classes
ClassPointer: ClassDef | Import;

ClassDef:
    scope=Scope 'class' name=ID ('extends' extends+=[ClassPointer])?
;
